name: iOS

# Only for changes to the iOS app, so the web app's CI is untouched by it
# and the other way round.
on:
  push:
    branches: [main, 'claude/**']
    paths: ['ios/**', '.github/workflows/ios.yml']
  pull_request:
    paths: ['ios/**', '.github/workflows/ios.yml']

concurrency:
  group: ios-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build and test (simulator)
    runs-on: macos-15
    timeout-minutes: 40
    defaults:
      run:
        working-directory: ios
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: |
          sudo xcode-select -s "$(ls -d /Applications/Xcode_16*.app | sort -V | tail -1)"
          xcodebuild -version

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate the project
        run: |
          # CI has no server: a placeholder key lets the app build; tests
          # do not talk to the network.
          printf 'SNATZEE_ANON_KEY = ci-placeholder\n' > Config/Local.xcconfig
          xcodegen generate

      - name: Pick a simulator
        id: sim
        run: |
          ID=$(xcrun simctl list devices available -j | python3 -c "
          import json, sys
          devices = json.load(sys.stdin)['devices']
          phones = [d for runtime, list_ in devices.items() if 'iOS' in runtime for d in list_ if d['name'].startswith('iPhone')]
          print(phones[-1]['udid'])")
          echo "id=$ID" >> "$GITHUB_OUTPUT"

      - name: Build and test
        run: |
          set -o pipefail
          xcodebuild test \
            -project Snatzee.xcodeproj \
            -scheme Snatzee \
            -destination "id=${{ steps.sim.outputs.id }}" \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            | tee xcodebuild.log | grep -E "error:|warning: .*Snatzee/|Test Case .* (passed|failed)|\*\* (BUILD|TEST) (SUCCEEDED|FAILED)" || true
          if ! grep -q "\*\* TEST SUCCEEDED \*\*" xcodebuild.log; then
            # Crashes and timeouts of the test host do not print "error:";
            # show what happened around them.
            grep -n -B3 -A12 -E "Restarting after|unexpected exit|crashed|Test crashed|timed out|with an uncaught|Fatal error|failed \(" xcodebuild.log | tail -150 || true
            exit 1
          fi

      # What TestFlight gets: the Release configuration for a real device,
      # so code behind #if DEBUG and the optimiser are covered too.
      - name: Release build (device)
        run: |
          set -o pipefail
          xcodebuild build \
            -project Snatzee.xcodeproj \
            -scheme Snatzee \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGNING_ALLOWED=NO \
            | tee release.log | grep -E "error:|warning: .*Snatzee/|\*\* BUILD (SUCCEEDED|FAILED)" || true
          grep -q "\*\* BUILD SUCCEEDED \*\*" release.log

      - name: Upload build log
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: xcodebuild-log
          path: |
            ios/xcodebuild.log
            ios/release.log
